# Copy these variables to Railway Dashboard - Variables Tab

# IMPORTANT: Change NODE_ENV to production for Railway deployment
NODE_ENV=production
PORT=3001
FRONTEND_URL=http://localhost:3000

# Database - Already has MongoDB Atlas connection
MONGODB_URI=mongodb+srv://yugiapp:2JHlycfUaThFOayP@yugiapp.ncak7q4.mongodb.net/yugi?retryWrites=true&w=majority&appName=YUGIAPP

# JWT Secret - IMPORTANT: Generate a new random secret for production!
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=30d

# Stripe (Leave as test keys for now, or update with live keys)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Email Configuration - UPDATE WITH YOUR GMAIL APP PASSWORD
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# File Upload
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Google Places API
GOOGLE_PLACES_API_KEY=AIzaSyDtB5YmjkJjZd_ubxLRhsW3LIspTfkFA1Y

# Foursquare API
FOURSQUARE_API_KEY=OWJRIKXWGCPIGM55OK5K33RCAR0Y2N1A0LQ4DB05OG2CSZBL


# ============================================================
# IMPORTANT NOTES:
# ============================================================
# 1. Change NODE_ENV to "production" before deploying
# 2. Generate a NEW random JWT_SECRET for production (min 32 chars)
# 3. Get a Gmail app password for EMAIL_PASS (not your regular password)
#    https://myaccount.google.com/apppasswords
# 4. Check if you need Firebase variables - check your backend/src/routes for Firebase usage
# 5. Whitelist Railway's IP in MongoDB Atlas if you encounter connection issues
# ============================================================

